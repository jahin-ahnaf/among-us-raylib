#include <bits/stdc++.h>
#include "objects/player.cpp"
#include "raylib.h"
using namespace std;

int main() {
    InitWindow(800, 600, "Something Cool");
    SetTargetFPS(60);

    while (!WindowShouldClose()) {
        BeginDrawing();
            
        EndDrawing();
    }

    CloseWindow();
    return 0;
}